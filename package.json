{
  "name": "poc-tower-battle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/check-data-mirror.mjs",
    "build": "tsc && vite build && node scripts/copy-levels-to-dist.mjs",
    "preview": "vite preview",
    "build:campaign": "node --experimental-strip-types scripts/build_campaign_v2.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^3.2.4"
  }
}
